import React from "react";

const Body = () => {
  return (
    <div //main body div
      style={{
        display: "flex",
        alignItems: "center",
        border: "solid",
        justifyContent: "center",
        background:"black",
        color:"white",
      }}
    >
      This is where the body belongs
    </div>
  );
};

export default Body;
